package ie.gmit.impressionengine.crawler;

import org.junit.Test;

public class LinksQueueTest {

	@Test
	public void testGetHeadOrAwait() {
		/*
		 * LinksQueue linkQueue = new LinksQueue(); linkQueue.add(new
		 * ComparableLink(100, "http://www.google.com", 0)); linkQueue.add(new
		 * ComparableLink(10000, "http://www.bing.com", 2)); linkQueue.add(new
		 * ComparableLink(1000, "http://www.yahoo.com", 1)); // Expected output:
		 * Bing, Yahoo, Google assertEquals("http://www.bing.com",
		 * linkQueue.getHeadOrAwait()); assertEquals("http://www.yahoo.com",
		 * linkQueue.getHeadOrAwait()); assertEquals("http://www.google.com",
		 * linkQueue.getHeadOrAwait());
		 */
	}

}
